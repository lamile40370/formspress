/**
 * Legacy editor entry — block registration moved to native block.json under
 * `/blocks/form` and `/blocks/flow-form`, each compiled to its own
 * `index.js`. This file remains only to preserve the editor SCSS bundle
 * (admin styles + builder iframe styles).
 */
import './editor.scss';
